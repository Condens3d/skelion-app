# Frontend build-time config (optional).
# Leave VITE_FORM_ENDPOINT unset to use the built-in API at /api/contact.
# Set it to a Formspree/Web3Forms URL for static-only deployments.
# VITE_FORM_ENDPOINT=

# Copy to .env (same folder as docker-compose.yml) before `docker compose up`.
# Generate SESSION_SECRET: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DB_PASSWORD=
SESSION_SECRET=
ADMIN_EMAIL=admin@skelionenterprises.com
ADMIN_PASSWORD=
